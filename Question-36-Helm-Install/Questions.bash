# Question: Helm Install

# Task
# 1. Add the Bitnami Helm repository (https://charts.bitnami.com/bitnami)
# 2. Install the nginx chart as release web-release in namespace
#    eda9e0ec987a-helm-test (create the namespace as part of the install)
#    with values:
#    - service.type=NodePort
#    - replicaCount=2

# Documentation Reference
# Helm Docs -> Using Helm
# https://helm.sh/docs/intro/using_helm/
