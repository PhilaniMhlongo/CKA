#!/bin/bash
set -e

echo "Nothing to pre-create for this question (everything goes in the default namespace)."
echo "[OK] Lab setup complete."
echo "You can now create the deployment and the NodePort service."
