# Question: ConfigMap as Volume

# Task
# 1. Create a ConfigMap app-config with key APP_COLOR=blue
# 2. Create a pod config-pod using nginx that mounts the ConfigMap
#    as a volume at /etc/config

# Documentation Reference
# Concepts -> Configuration -> ConfigMaps
# https://kubernetes.io/docs/concepts/configuration/configmap/
